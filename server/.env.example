PORT=4000
CLIENT_URL=http://localhost:3000

# Create an OAuth App at https://github.com/settings/developers
# Authorization callback URL: http://localhost:3000/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# Site manager. Opening a site runs that repo's own scripts on this server, so
# limit who can do it. Comma-separated GitHub logins; if empty, everyone may in
# development and no one may when NODE_ENV=production.
ALLOWED_GITHUB_LOGINS=
# Where site working copies are cloned (default: <os tmpdir>/twinstack-workspaces).
# Uncommitted work lives here, so use persistent disk in production.
WORKSPACES_DIR=
